function us_prim(n)
{
    for (var i = 2 ; i * i <= n; i++)
        if (n % i == 0 )
        return false;
}

for (var i = le9; i < le10; i++   )
if (is_prim(i))

postMessage(i) ;

postMessage("gata");